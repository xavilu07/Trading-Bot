# Quantum Bot Dashboard

Reserved workspace for the future React frontend.

The foundation phase intentionally contains no frontend runtime, dependencies,
performance metrics, build output, or deployment configuration. The future
development server is reserved for `127.0.0.1:3101`.

The frontend must consume only the isolated read-only API under `/api/v1`.
